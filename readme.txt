Pasos:

Asegúrate de tener Node y Angular CLI instalados:

node -v
npm -v
ng version


En la carpeta del proyecto (donde está package.json), ejecuta:

npm install


Esto descargará automáticamente todas las dependencias necesarias dentro de node_modules.

Luego ya puedes correr el proyecto:

ng serve